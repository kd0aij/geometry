package:
  name: geometry
  version: "0.0.1"

build:
  number: 0

source:
  path: ./

requirements:
  build:
    - python  {{python}}
    - setuptools
  host:
    - python
  run:
    - python
test:
  imports:
    - geometry